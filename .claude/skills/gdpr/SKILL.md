---
name: gdpr
description: Инженерный privacy/GDPR-дистиллят Matio — карта персональных данных, чеклист обязательств для новой фичи (минимизация, ретеншен, стирание, экспорт, бэкапы, логи, процессоры), триггеры «зови человека». Использовать при ЛЮБОЙ задаче, трогающей персональные данные — новое поле/таблица/кеш-ключ с данными пользователя, новый внешний сервис или SDK, изменение писем/пушей/аналитических событий, ретеншен, удаление/экспорт аккаунта, инцидент с данными.
---

# GDPR: инженерный дистиллят Matio

**ЖИВОЙ документ**: изменил обработку данных (новое поле/хранилище/поток,
новый процессор, ретеншен, содержимое писем/пушей/событий аналитики) →
обнови этот скилл и его `references/` ТЕМ ЖЕ PR. Правило зафиксировано в
CLAUDE.md («Core rules → Personal data changes run through `/gdpr`»).

**Не юридическая консультация.** Выбор правовых оснований, тексты согласий
и политики, DPIA и юридическая приёмка перед публичным запуском — за
человеком (владелец + юрист). Скилл отвечает на инженерные вопросы каждого
PR: данные не размножаются бесконтрольно, стирание реально стирает, логи
чисты.

Справки рядом:
`references/data-map.md` (карта данных — что где живёт, какие поля, как
стирается, ретеншен, дыры) · `references/processors.md` (кто видит данные,
регион, DPA, механизм трансфера, дата сверки).

Что уже есть в кодовой базе по теме (не изобретать заново): HMAC-хеш IP
вместо сырого (`lib/trial.ts:hashClientIp`, тот же приём в
`show_reminders.ip_hash`, `guest_checkout_attempts`); стирание CAPI-снимка
(сырой IP/UA, `_fbp`/`_fbc`) из метаданных Stripe сразу после `Purchase`
(`lib/subscription-mirror.ts`, #165); скрабберы Sentry
(`lib/observability.ts`) и лог-аудит (`lib/log-audit.test.ts`); гейт
маркетингового согласия в одном месте (`proxy.ts` + `lib/cookie-consent.ts`);
отписка от писем с удалением всех строк адреса (`lib/email-unsubscribe.ts`);
шифрованные бэкапы с ретеншеном 35 дней (`infra/backup/`).

## Правовая рамка Matio — ЧЕРНОВИК, ДО РЕВЬЮ ЮРИСТОМ

<!-- Заполнено агентом 06.09.2026 по ответам владельца на три вопроса модуля
и по факту кодовой базы. Ни один пункт не является юридическим выводом:
спорные места перечислены в теле PR этапа 10 отдельным списком. Юрист
подтверждает или переписывает раздел целиком; до этого раздел — гипотеза. -->

1. **Контролёр** — DEEP ORDINARY LTD (Англия и Уэльс, № 17381666, 66 Paul
   Street, London EC2A 4NA), DPO не назначен. Применимы **оба** режима: UK
   GDPR (учреждение в UK) и EU GDPR по ст. 3(2) — сайт es/en, Испания —
   ключевой рынок, зрители из ЕС есть. Надзорные органы, названные в
   `/privacy`: ICO и AEPD. Открытые вопросы юристу: представитель в ЕС
   (ст. 27) для UK-контролёра без учреждения в ЕС; ICO-сбор — уже в #87.
2. **Особых категорий (ст. 9) нет.** Продукт — стриминг сериалов: email,
   история и позиция просмотра, страна (ISO-2 из IP), платёжные реквизиты у
   Stripe. Косвенных раскрытий (здоровье, убеждения) в данных нет; ИИ-инференса
   по данным зрителей нет. Ответ владельца 06.09.2026.
3. **ИИ-обработки пользовательского контента нет.** ИИ используется студией
   для производства сериалов, не над данными зрителей; пользователи ничего не
   загружают и не пишут (нет комментариев/сообщений). LLM-промптов с данными
   пользователей в коде нет (`grep -rniE "openai|chatgpt"` находит только
   рекламный пиксель ChatGPT Ads). Появление первого LLM-вызова над данными
   пользователей = решение владельца + новый процессор + абзац в карте.
4. **DPIA (ст. 35, критерии WP248)** — пограничный случай, решает юрист. Из
   девяти критериев могут сработать два: «систематический мониторинг»
   (пиксели Meta/Google/OpenAI + session replay PostHog, все — только по
   согласию) и «сопоставление наборов данных» (склейка `visitors`→`users`,
   кросс-вендорные конверсии по хешированному email). Особых категорий,
   уязвимых субъектов, инновационных технологий над данными, крупного
   масштаба — нет. Инженерная половина DPIA (карта данных, процессоры,
   потоки) уже готова в `references/`.
5. **ePrivacy / PECR / LSSI (cookies).** Баннер с двумя равными кнопками и
   ничего маркетингового до «Accept all» — только для EU/EEA/UK/CH
   (`marketingConsentRequired`); вне этих стран маркетинговое согласие
   включено по умолчанию решением владельца (LatAm — ключевая аудитория;
   LGPD/Бразилия и прочие режимы — вопрос юристу). Первичная
   аудитория `matio_aid` (13 месяцев, без продления, только своя аудитория,
   без кросс-сайта) оформлена как consent-exempt по логике CNIL; позиция AEPD
   по аналитическим cookie уже — вопрос юристу для ключевого рынка.
   Письма-напоминания — явный запрос через форму, отписка в один клик
   (RFC 8058) в каждом письме.
6. **Правовые основания (черновик).** Договор, ст. 6(1)(b): аккаунт,
   воспроизведение, прогресс, биллинг. Законный интерес, 6(1)(f): первичная
   аудитория, антиабьюз (ip_hash, rate limits), безопасность, бэкапы.
   Согласие, 6(1)(a): маркетинговые пиксели, PostHog, Mux Data, GA4,
   напоминания. Юридическая обязанность, 6(1)(c): налоговые записи (у Stripe).
7. **Возраст.** Проверки возраста нет: регистрация в Clerk без даты рождения.
   Контент студии не маркирован как 18+ в коде. Вопрос юристу: нужен ли гейт
   (ст. 8 — 16 лет в ЕС, 14 в Испании, 13 в UK) и в каком виде (чекбокс, не
   дата рождения — минимизация).

## Чеклист «новая фича → обязательства» (прогоняй в каждом PR с данными)

- **Новое поле/таблица/кеш-ключ с данными пользователя?**
  - Минимизация (ст. 5(1)(c)): зачем; можно ли меньше/короче/хешем/без?
    Образцы в репо: `ip_hash` вместо IP, `country` вместо гео-точки,
    `session_token` — случайный UUID, `visitors.referrer` обрезан до 300.
  - Ретеншен (ст. 5(1)(e)): «бессрочно» — не дефолт, а решение. Сегодня
    джоб чистки нет вообще (#162) — новая таблица называет своё окно в
    колонке «Ретеншен» карты и не усугубляет дыру.
  - Стирание: FK на `users.id` с `onDelete: "cascade"` (образец —
    `watch_progress`), либо `set null` с обоснованием, почему строка должна
    пережить аккаунт псевдонимной (образец — `visitors`). Таблица без FK на
    `users`, но с персональными полями (как `show_reminders.email`) обязана
    ответить в карте, чем она стирается.
  - Экспорт: попадёт ли в выгрузку по ст. 15/20 (#163)? Персональное — да.
  - Карта: строка в `references/data-map.md` ТЕМ ЖЕ PR.
- **Новый внешний сервис/SDK/эндпойнт, куда уходят данные?** → это
  процессор (ст. 28): DPA до прод-данных, механизм трансфера (DPF/SCC),
  строка в `references/processors.md` + `docs/registry.md` (пока DPA не
  подтверждён). Новый вид данных наружу — сперва слово владельца. Пуши для
  приложения (#98) — процессор Expo/FCM/APNs появляется в таблице ДО того,
  как уйдёт первый пуш.
- **Меняешь письма/пуши/события аналитики?** Их содержимое видят провайдеры
  (Resend видит адрес + тему + тело; PostHog — свойства событий и `email`
  персоны; пиксели — id событий). В письма — только контент шоу (название,
  эпизод, логлайн, кадр), никаких данных пользователя кроме адреса; в
  свойства событий — id, слаги, номера, режимы, но не email/имена/тексты
  (`lib/posthog-events.ts`, `lib/openai-pixel-events.ts`). Обнови абзац «Что
  уходит во внешние сервисы» в карте.
- **LLM-промпты?** Сегодня их нет. Первый LLM-вызов над данными
  пользователей = решение владельца, новый процессор, абзац «Что уходит в
  LLM» в карте. Отправлять минимум (email/id — нет), в пределах лимитов
  конфига.
- **Логи/трекер ошибок/аналитика** — правило CLAUDE.md: данные
  пользователей НИКОГДА; только id, статусы, длительности. Новый серверный
  путь, который логирует, получает кейс в `lib/log-audit.test.ts` ТЕМ ЖЕ PR.
  Sentry: скрабберы в `lib/observability.ts` — новое поле в payload
  ошибок = кейс в их сьюте.
- **Медиа/файлы?** Пользователи сегодня ничего не загружают (Blob хранит
  только артворк шоу и аватары виртуальных актёров). Появится аватар/загрузка
  пользователя → EXIF-стрип на пайплайне, путь в Blob привязан к `users.id`,
  удаление объекта — явный шаг каскада (Blob не знает о наших FK).
- **Черновики/джобы/кеши с контентом?** Наследуют чувствительность
  оригинала: тот же запрет на логи, ретеншен, каскад. Сегодня кешей с
  персональными данными нет (`roleCache` в `proxy.ts` — 5 с, только роль).
- **Инструменты разработки с доступом к проду.** `.env.local` и Neon MCP
  смотрят в боевую базу. Персональные колонки (`email`, `referrer`,
  `session_token`) не выгружаются в чат/контекст агента — только id,
  счётчики, агрегаты. Вопрос «а какие адреса подписались» решается
  `count(*)`, не `select email`.

## Канон стирания (ст. 17) — как это устроено по факту

Факт на 06.09.2026 (полная таблица — `references/data-map.md`):

- **Триггер** — удаление аккаунта в Clerk (UserProfile → «Delete account»,
  дашборд, или наш ручной запрос). Вебхук `user.deleted`
  (`app/api/webhooks/clerk/route.ts`, с #161) — единственная механика:
  `DELETE FROM users WHERE id = <clerk id>` → каскады FK + явное удаление
  `show_reminders` по адресу аккаунта. Идемпотентно: повтор для уже стёртого
  — 200 без записи. Ручной GDPR-запрос исполняется удалением в Clerk тем же
  путём, не отдельным SQL. Ops-условие: прод-эндпойнт в дашборде Clerk должен
  быть подписан на `user.deleted` (пока нет — строка в `docs/registry.md`).
  Тест `app/api/webhooks/clerk/route.test.ts` пришпиливает полную карту FK на
  `users`: новая таблица со ссылкой на `users` без `onDelete` ломает тест —
  и это правильно, потому что иначе она ломала бы стирание. Таблица БЕЗ FK
  на `users` карту не меняет — так устроен `erased_customers` (тумбстоун
  обязан пережить DELETE, поэтому ключ — Stripe customer id, не user id);
  тот же тест пришпиливает, что у него FK нет вовсе.
- **Каскады FK от `users.id`** (`db/schema/*`, проверено по коду):
  `CASCADE` — `subscriptions`, `watch_progress`, `watch_days`; `SET NULL` —
  `trial_sessions.user_id`, `visitors.user_id` (история визитов остаётся
  псевдонимной: aid, referrer, utm, country), `show_reminders.user_id` — но на
  этот SET NULL обработчик не полагается: строки `show_reminders` по адресу
  аккаунта и по `user_id` он удаляет сам, ДО `DELETE FROM users` («меня
  больше нет» включает напоминания; SET NULL остаётся страховкой схемы),
  `marketing_links.created_by` (админ).
- **Не привязано к `users` вовсе** (псевдонимное): `trial_sessions` без
  `user_id` (session_token, ip_hash), `visitors` без `user_id`,
  `guest_checkout_attempts` (ip_hash, самопрунинг 2 ч), `stripe_events`
  (id событий).
- **Кеши**: `roleCache` (5 с, process-local) истекает сам; Data Cache —
  каталог и агрегаты без PII. Чистить нечего.
- **Файлы/медиа**: у пользователей нет загрузок. Чистить нечего.
- **Джобы**: фоновых джоб с данными пользователей нет; рассылка — ручная
  кнопка в админке, читает `show_reminders` в момент нажатия.
- **Процессоры**: Clerk — источник истины. Stripe — обработчик
  `user.deleted` сам ставит живой подписке `cancel_at_period_end: true`
  (best-effort: сбой Stripe стирание не останавливает, но уходит в лог и
  Sentry по id) и ДО `DELETE FROM users` пишет `stripe_customer_id` в
  тумбстоун `erased_customers` — без него следующий вебхук Stripe по этому
  клиенту (`guest = "1"` в метаданных не истекает) воссоздал бы Clerk-
  пользователя и `users` с адресом через `claimGuestCheckout`;
  `mirrorSubscription` и `/welcome` спрашивают тумбстоун ПЕРЕД claim.
  Stripe Customer (email, billing address) при этом остаётся — `customers.del`
  = решение владельца (`docs/registry.md`); PostHog person (с `email`)
  требует отдельного вызова (хвост #164); Resend — логи
  истекают через 30 дней сами; Meta/Google/OpenAI держат хешированный email
  и клиентские id событий — per-user удаления у них нет; Sentry — только
  `user.id`, истекает по ретеншену плана.
- **Бэкапы не вычищаем точечно** — позиция ICO «put beyond use»: live-системы
  чистятся сразу; дампы (age-шифрование, приватный Vercel Blob, Франкфурт)
  уезжают ретеншеном 35 дней (`infra/backup/blob.ts prune`); restore из
  бэкапа после erasure → повторный прогон erasure — шаг §7 в
  `docs/runbooks/db-restore.md`, но **реестра заявок, по которому его
  прогонять, нет** (#164). Neon PITR — 6 часов, короче любого окна.
- **Устройство пользователя** (cookies, localStorage, SecureStore
  приложения) — вне нашего контроля; `/cookies` объясняет, как удалить.

## Канон портируемости (ст. 20) и доступа (ст. 15)

- Сегодня ни скрипта, ни ранбука (#163): выгрузка — SQL руками по
  `users` → `subscriptions`, `watch_progress`, `watch_days`,
  `trial_sessions`, `visitors` + `visitor_days` (по `user_id`) и
  `show_reminders` (по `email`) плюс выгрузки из Clerk, Stripe (портал уже
  отдаёт инвойсы) и PostHog (person + события по `distinct_id` = Clerk id).
- Формат: JSON-архив — «структурированный, машиночитаемый»; ссылка с
  ограниченным TTL, когда появится место для приватных объектов.
- Derived-данные (страна, атрибуция, `converted`, флаги дней) в ст. 20 не
  входят, но доступ по ст. 15 шире — экспорт проектируется так, чтобы их
  можно было приложить.

## Инцидент с данными (ст. 33/34) — только эскалация

Признаки: доступ посторонних к Neon, к бэкапам в Blob или к приватному
age-ключу (`BACKUP_AGE_SECRET_KEY` + менеджер паролей владельца), утёкший
`.env.local` (в нём боевые `DATABASE_URL`/`sk_live`), данные пользователей в
открытых логах Vercel, в Sentry, в событиях PostHog/пикселей, письмо не тому
адресату, персональные данные в теле issue/PR/чата агента. Часы (72 ч на
нотификацию SA) тикают с момента **awareness**.

Действия агента: немедленный доклад владельцу (p1, не в дайджесте) + issue
`type:bug` + `domain:infra` + `p1` + зафиксировать таймлайн и масштаб (какие
таблицы/ключи, сколько субъектов — `count(*)`, не списки). Решения
«рисково/не рисково» и нотификации (ICO/AEPD, субъекты) — человек. Любой
инцидент, даже ненотифицируемый, — во внутренний реестр (ст. 33(5)): issue +
строка в post-mortem.

## Триггеры «зови человека» (агент не решает сам)

DPIA и его выводы · выбор/смена правового основания, тексты согласий и
политик (`/privacy`, `/cookies`, `/terms` — все три «до ревью юристом») ·
новые категории данных наружу (первый LLM-промпт, новый процессор, новое
поле в письме/пуше/событии) · ослабление приватности по умолчанию (гео-дефолт
согласия, session replay без маски, `sendDefaultPii`) · возрастная проверка ·
breach (ст. 33/34) · юрлицо-контролёр, представитель в ЕС (ст. 27), DPO
(ст. 37), ROPA · маркетинг/трекинг/ATT (пуши и трекинг в приложении, #98) ·
юридическая приёмка запуска платежей (#151).

## Источники

- Текст GDPR: https://eur-lex.europa.eu/eli/reg/2016/679/oj (по статьям —
  https://gdpr-info.eu/); ст. 9, 17, 20, 25, 28, 33–35 — ядро.
- DPIA: WP248 rev.01; портируемость: WP242 rev.01 —
  https://www.edpb.europa.eu/our-work-tools/general-guidance/endorsed-wp29-guidelines_en
- Breach: EDPB Guidelines 9/2022; ИИ-модели: EDPB Opinion 28/2024.
- Реестр DPF (сверять процессоров): https://www.dataprivacyframework.gov/list
  — страница на JS, из агента не читается; сверка по публичным страницам
  вендоров с датой — в `references/processors.md`.
- Плейбук: `docs/mega-process/10-privacy-gdpr.md`.
