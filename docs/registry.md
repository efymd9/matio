# Registry of loose ends

Every stub, placeholder, hard-coded shortcut, and deliberately deferred piece of
work gets a row here **in the same PR that introduces it**. The point is that
"we'll do it later" survives outside anyone's memory: the registry is read
before a release (see the `/release` skill) and during review of every PR.

A row leaves the table only when the thing is actually done — or when a real
issue on the board takes over tracking it (put the issue number in "When to
close").

| What | What it blocks | When to close |
|---|---|---|
| **Branded Clerk emails built but not applied** (#276): the eight account emails in the owner-picked Matio design sit in `infra/clerk/email-templates/`; production still sends Clerk's default layout (with our name, logo and support address), because custom email templates are a Clerk **Pro** feature ($25/mo) and the instance is on the free plan | The sign-in code — the email every viewer gets — looks like Clerk, not Matio | When the owner takes Clerk Pro and the templates are applied by the README's steps (wordmark live first, `verification_code` first), or the owner decides the default stays |
| **App Store seller is a private individual** (#99): the iOS app is built and submitted under the owner's Individual Apple team `MTFRZQ8SRX`, so the store page names a person, not DEEP ORDINARY LTD | brand/legal consistency with #87 (vendor migration to the LTD) | App Transfer to an Organization account (D-U-N-S for the LTD, Apple verification 1–2 weeks) BEFORE the public App Store submission; internal TestFlight is fine as is |
| **Android release path not set up** (#99): no Google Play Console ($25), no keystore yet | Android testers and release | Owner opens Play Console; first `eas build -p android` generates the keystore → back it up from `eas credentials` the same day (a lost keystore = the app can never be updated) |
| **Store artefacts not produced** (#99): icons exist, but no screenshots, es/en store copy, privacy nutrition labels, age rating, ATT decision | public submission | After the first TestFlight build is installed and the owner decides the catalog is ready |
| Backups exist but have never run against the live database: `db-backup` / `db-restore-check` ship here, and the loop was rehearsed end-to-end only against a local Postgres 18 with synthetic data. The Neon `production` branch is also still unprotected (a vendor-panel switch) | Until both workflows have gone green once from `main`, the restore probe proves the *scripts*, not this database's dumps | When `db-backup` and `db-restore-check` have each run green from `main` (the probe logs «OK: restore-проба пройдена …») and the Neon branch is marked protected |
| Only Postgres is backed up. Show artwork in Vercel Blob and video in Mux are not: the database holds links, the files live at the vendors | Losing the Blob store = a catalog without posters; losing the Mux account = a catalog without video. Both survive a database restore untouched | When the artwork bucket gets its own copy (or the owner accepts the risk in writing) |
| `a11y` addon runs in `todo` mode — violations are visible in the Lab but do not fail CI | Nothing today; it is a gate that is deliberately off | After the existing components are cleaned up (see the design-system debt issue) |
| Mobile app (`mobile/`) is not versioned by release-please — it lives outside `main` | Product and app versions will diverge once the app ships | When `feat/mobile-app` merges: add `mobile/app.json` to `extra-files` |
| The staging bench is closed by an in-app password (`STAGING_LOCK_PASSWORD`, HTTP Basic Auth in `proxy.ts`) instead of Vercel Authentication, which refuses the project's main alias on the current plan (`428 … not available on your plan for production deployments`). Consequences: one shared credential resent on every request with no per-person revocation, and only what passes through `proxy.ts` is covered — `_next/*` and extension-matched static files are outside its matcher and answer without the password | Nothing today: the bench holds seeded data only — no accounts, no viewing history, no email addresses, and no secrets in the bundle | When the bench gets platform-level protection (Vercel Pro's Vercel Authentication over the main alias, or its own domain behind an access proxy) — then the variable is removed and the code path goes dormant |
| Sentry source maps are off: `SENTRY_ORG` / `SENTRY_PROJECT` / `SENTRY_AUTH_TOKEN` are unset, so prod stack traces name minified chunks | Every real stack trace costs extra work to read | When the three build-time variables are set in Vercel Production and a prod event shows a readable frame |
| `/api/readyz` exists but **nothing polls it**: there is no uptime monitor and no status page, so a red readiness answer is only seen by whoever runs `curl` | Nothing today — the release workflow's smoke test uses `/api/healthz` — but a database outage between deploys goes unnoticed until a page fails | When an external check polls `/api/readyz` (the playbook's optional Cloudflare-Worker status page, or any uptime monitor) and its failures reach the owner |
| `docs/mega-process/tools/pr_watcher.sh` has diverged from the upstream playbook: it carries the `UNKNOWN`-is-not-an-event fix (#54) that the source copy does not have | Refreshing the playbook copy from upstream would silently restore the bug — false wake-ups on every mergeability recompute, and by then nobody remembers why | When the fix lands upstream in the playbook, or when we declare `docs/mega-process/` our own fork and stop expecting refreshes from the source |
| Загрузка видео в админке **не возобновляется**: прогресс upchunk живёт только в памяти вкладки, поэтому перезагрузка страницы или закрытая вкладка отбрасывают многогигабайтную загрузку на ноль. Смягчено предупреждением `beforeunload` и ретраями по обрыву (#130), но самой возобновляемости нет | Монтажёр, у которого связь оборвалась насовсем, начинает с нуля; на плохом канале это может не сойтись никогда | Когда появится возобновление — либо через хранение позиции резюмируемой сессии между перезагрузками, либо переходом на инструмент, который это умеет из коробки |
| **The Expo app has no working linter.** `mobile/package.json` declares `"lint": "expo lint"`, but the app ships neither an ESLint config of its own nor any ESLint dependency, so the command walks up to the repo-root config — which ignores `mobile/**` — and dies with "all of the files … are ignored". Found 20.08 while merging #17; it has never worked | Nothing in CI (the web lint deliberately ignores `mobile/**`, so nothing regresses), but React Native source is currently unlinted — the rules that would catch a missed hook dependency or an unused import in the app simply do not run | When `mobile/` gets its own flat config plus the ESLint toolchain (`eslint-config-expo`) — a dependency addition, so it needs the owner's go-ahead per CLAUDE.md. #96 touched the app source without it (the go-ahead was asked for in that PR's thread, not assumed); lands in the follow-up that gets it |
| The app's device-language detection (`mobile/src/i18n/locale.tsx`) is unverified on hardware: it reads `I18nManager.getConstants().localeIdentifier` (Android) and falls back to Hermes' `Intl.DateTimeFormat().resolvedOptions().locale` (iOS), through the web's `pickFromLanguageTags` — chosen over `expo-localization` because that is a new dependency. No simulator was reachable in the #96 session | A Spanish-language phone might open in English until the user taps ES in the header (the choice then persists) — a first-impression cost, not a breakage | When the owner sees Spanish on a Spanish-locale iPhone AND Android with no switch tapped; if either shows English, add `expo-localization` (with approval) and read `getLocales()[0].languageTag` first |
| The app saves no progress for anonymous viewers: `POST /api/v1/progress` is signed-in only (per the #96 spec), while the web also tracks anonymous positions in `trial_sessions` by cookie. Under the app's `after_episodes: 1` gate an anonymous viewer watches episode 1 without an account | Closing the app mid-episode-1 signed out restarts it from zero, and the continue-watching rail is empty until sign-in | When a device-keyed anonymous position write exists (the `trial_sessions` row the token route already mints per device is the natural home — `saveTrialPosition`'s free branch, keyed on the device id instead of the cookie), or the owner decides the signed-in-only rail is enough |
| The app's vertical chrome (`mobile/src/components/vertical-chrome.tsx`) has tap-to-play/pause, progress read-out and sound only — no scrubbing, no episodes sheet, no share, no "tap for sound" pill, unlike the web's `vertical-chrome.tsx` | A vertical show can be watched, resumed, swiped between episodes (#97) but not seeked from the app; the native transport is off for these shows on purpose (it would draw a landscape-shaped scrubber over a portrait stream) | When the vertical chrome gets a scrubber (the #97 spec covered the feed, auto-advance, buckets, PiP — not scrubbing); a follow-up issue at dispatch |
| **Chromecast is not in the app.** `/v1/config` reports `flags.castEnabled`, the player sets `allowsExternalPlayback` (AirPlay via the native transport's route button on landscape shows, Control Center on vertical ones — no in-chrome AirPlay picker without a native module), but there is no Cast SDK: `react-native-google-cast` is a new dependency and a separate receiver client with its own token-TTL question (`docs/mobile-app-plan.md` §9) | An Android/Chromecast household can only AirPlay-less mirror; `castEnabled` is a flag with no consumer | Phase 4 of the plan (§12: offline downloads + Chromecast) — an issue at dispatch; the dependency needs the owner's go-ahead |
| **No PostHog in the app**, so the web's `episode_auto_advanced` (and every other funnel event) has no mobile twin — the feed's auto-advance is measured only through `watch_segments` / `watch_progress` (#97) | The PostHog funnels read web-only; app engagement exists only in the first-party ledgers until phase 5 (`platform` dimension) | When the app gets an analytics client (phase 5, §12 — `posthog-react-native` is a new dependency, owner's go-ahead) |
| **The app's retention-bucket queue is process-lifetime only** (`mobile/src/watch/segment-queue.ts`): a flush that fails for a network reason waits in memory with backoff and is retried on foreground, but an app KILLED while offline loses what was queued. The only store the app has is the keychain (SecureStore, ~2KB/value) — wrong place and too small for behavioural data; persisting needs `expo-file-system` or AsyncStorage | Retention curves under-count viewers who lose the network and then swipe the app away before it returns — a few buckets per such session, never a wrong shape | When a persistence dependency is approved (owner's call) — then the queue serialises on enqueue and replays on launch |
| **The Account tab always says «Member»** (`mobile/src/app/(tabs)/account.tsx`, #245). `/v1` carries no subscription state — neither `/v1/config` nor `/v1/continue` says whether the signed-in user holds an access-granting `subscriptions` row — so the app cannot honestly print «Subscriber», and its paid-mode gating (`hasSubscription = false` in `show/[slug].tsx`, `watch/episode-feed.tsx` and `watch/first-episode.ts`) treats every signed-in viewer as a non-subscriber | A paying subscriber sees «Member» on the Account tab, and the app walls `subscriber`-tier episodes for them client-side (the token route would allow them). Presentation + a wasted wall; no billing exposure | When `/v1/config` (or a `/v1/me`) carries the subscription status — an additive field on the contract; then the pill reads «Subscriber» and the three `hasSubscription = false` sites read it |
| **«Manage subscription» is hidden in the app** (`mobile/src/app/(tabs)/account.tsx`, #292). The row opened matio.tv's homepage in an in-app browser — signed out (its own cookie jar), and for free members too, who have nothing to manage; the owner's call (25.09) is to hide it until `/v1` says who is a subscriber (the row above). Cancelling stays on the site: matio.tv → «Manage subscription» → the Stripe portal | A paying subscriber finds no way to manage the subscription inside the app, only on the web | When `/v1` carries the subscription status (row above): show the row to subscribers only, pointed at `${urls.web}/api/billing-portal` (a Clerk sign-in in the browser) — or whatever the owner decides for #99's App Store 3.1 review |
| **The update wall opens TestFlight** (`mobile/src/api/config-context.tsx`, #292): «Update» opens `itms-beta://`, falling back to `https://testflight.apple.com/` — right while the app ships through TestFlight only, wrong once it is in the App Store (and meaningless on Android, which has no build yet) | A store user retired by `minSupportedBuild` would be sent to TestFlight instead of the store page | At the public release (#99): point it at the store page (`https://apps.apple.com/app/id6812321366`, and Google Play for Android) — best as an additive `urls.store` in `/v1/config`, so a shipped build can be re-pointed without a release |
| **Autoplay off rests on the ended player, not on the next episode's poster** (`mobile/src/watch/episode-feed.tsx`, #245). With Settings → Playback → «Play next episode automatically» off, a landscape show's `ended` simply does not advance: the page keeps the native transport's own end state (replay), and the next episode is reached from the show page. The board's «next poster with a play button» needs the neighbour page to arrive paused — a change deeper than the one check at `ended` the spec allowed | One extra tap (back → next episode) for a viewer who switched autoplay off; the default (on) is untouched | A follow-up that gives `FeedPage` an «arrive paused» prop and a play glyph on the landscape chrome — small, but a player change with its own spec |
| **No rate limit on the retention flush** — neither `POST /api/v1/watch-segments` nor the web's `saveWatchSegments` action (cookie path) bounds how often one identity may post buckets; the only brakes are the per-flush cap (120) and the session-row / positional-gate proof. Parity with the web as shipped, not a decision (#97 review) | A scripted client holding one valid session row can inflate an episode's curve by looping flushes — the curve is a relative shape, so the damage is a skewed picture, not data loss | When a per-`(ip_hash, day)` limit lands — for BOTH surfaces in the same PR (the write is shared in `lib/watch-segments-write.ts`, so the limit belongs there, not in one caller); a `tech` issue at dispatch |
| Clerk `authorizedParties` (#100) is proved by unit tests only. Two distinct exposures. **The list**: garbage in a variable is merely skipped (safe), but a VALID foreign value is not — a list without the deployment's real origin makes `TokenInvalidAuthorizedParties` (not a handshake reason) sign every request out: the SignupWall for everyone under `REQUIRE_SIGNUP`, a redirect loop on `/admin`. Prod is covered by the hard-pinned `SITE_URL` always being in the list; the bench's origin comes from `VERCEL_PROJECT_PRODUCTION_URL` alone, so a wrong value there signs the BENCH out. **The `/api/v1` exemption** for azp-less header tokens rests on a documented SDK property — `@clerk/expo` session tokens carry no `azp` — that this repo has not observed on the wire | Nothing today | (1) Browser, doable on the bench right after the first staging deploy carrying it: sign in on `matio-staging.vercel.app`, stay signed in across reloads, open `/admin` as a bench admin. The lock's `Basic` header is not a token to Clerk, so this exercises the cookie path WITH the list. (2) Native, NOT doable on the bench: it sits behind Basic Auth (`lib/staging-lock.ts` opens only `/api/healthz`) and `Authorization` carries either `Basic` or `Bearer`, so no Bearer call reaches `/api/v1` there. The proof is the owner's, with #99: in a dev build of the app decode the payload of a real `getToken()` (base64url middle segment) and confirm it has no `azp`. Until then the exemption is unverified live. Row leaves once both are seen |
| `lib/flags.ts` is dead code: it exports `showLanding` with **zero call sites**; `SHOW_LANDING=true` sits in `.env.example` and a typo'd `truei` in `.env.local` | Nothing — but a new session greps it, believes a "landing flag" exists and wires it somewhere | When the module and both env mentions are deleted (one-line cleanup PR), or a landing actually appears |
| The /press kit ships a reduced bundle: wordmark + logomark as **PNG only** (no SVG sources exist), **4** stills (the mock promised «12 × 4K» — only one is 4K) and **no fact-sheet PDF**; the mock's fact-sheet card is deliberately not rendered and the card labels describe the real contents | Nothing functional — the page is honest about what the ZIP holds — but journalists get a thinner kit than the design intended | When the owner produces the missing materials (SVG marks, more 4K stills, a 2-page fact sheet) and the kit + card labels are regenerated |
| **Стирание аккаунта: прод-эндпойнт Clerk не подписан на `user.deleted`** — ops-шаг владельца. Код (#161) стирает `users` + каскады + `show_reminders` по адресу, но эндпойнт `https://matio.tv/api/webhooks/clerk` создавался с одним `user.created`; пока событие не отмечено в дашборде, удаление аккаунта в Clerk по-прежнему не доходит до БД | Ст. 17 исполняется только для событий, которые Clerk реально шлёт; `/privacy` обещает «account … plus a short grace period after deletion» | Когда в Clerk Dashboard → Webhooks → endpoint отмечен `user.deleted` и тестовое удаление даёт `users = 0` (рецепт в `docs/operations.md`) — комментарий в #161 |
| On `/watch`, marketing consent **granted mid-episode** starts Mux Data only at the next `initialize()` of the element — the auto-advance src swap, the hourly token-refresh remount or a manual episode swap — not on the spot (#127). `@mux/mux-video-react` reads the Mux Data props once, at initialize, and the only ways to start a monitor on a running element are a stream reload (the #126 hiccup) or re-implementing playback-core's `setupMux` over `mux-embed` in our code. Withdrawal is NOT deferred — it stops the monitor immediately | Measurement only: a viewer who accepts cookies mid-episode is unmeasured by Mux Data for the rest of that element's life (≤1h, or until the next episode). No privacy exposure — nothing fires before consent | When the wrapper (or playback-core) exposes a way to start monitoring on a mounted element without reloading the stream, or when the owner decides the mid-episode reload hiccup is an acceptable price for full measurement |
| **Экспорт данных субъекта (ст. 15/20) — только CLI** (`pnpm export-user-data`, #163): self-service «скачать мои данные» в аккаунте нет — заявка идёт письмом на contact@matio.tv и исполняется оператором по `docs/runbooks/gdpr-requests.md`; адрес без аккаунта (только строки `show_reminders`) скрипт не покрывает — руками по ранбуку; вендорские половины best-effort (`skipped` в сводке = выгрузить в дашборде вендора) | Ничего сегодня: 30-дневный срок ст. 12(3) покрывается ручным прогоном; растёт цена каждой заявки, не риск | Продуктовое решение владельца: кнопка «Download my data» в аккаунте (нужно место для приватных объектов со ссылкой с TTL) — либо «остаёмся на CLI», и строка уходит с пометкой «принято» |
| **`show_reminders` без индекса по `notified_at`** (#162): подзапрос крона ретеншена по отправленным строкам идёт seq-scan с `LIMIT 1000`; частичный `show_reminders_show_id_pending_idx` (`WHERE notified_at IS NULL`) ему не служит — обратная популяция | Ничего сегодня: в таблице десятки строк (капча 10/IP/час), seq-scan дешевле миграции в горячем `drizzle/` | Когда таблица перевалит ~10⁴ строк — частичный индекс `(notified_at) WHERE notified_at IS NOT NULL` одной expand-миграцией (обоснование блокировки — в `.sql`, по правилу CLAUDE.md) |
| **`stripe_events` растёт бессрочно** — намеренно вне крона ретеншена (#162): таблица держит только id событий Stripe для идемпотентности, персональных данных нет, и окно для неё — не privacy-, а ops-решение владельца (в issue черновик — 90 дней; риск обратной стороны: Stripe переигрывает событие старше окна → обработчик увидит его как новое) | Ничего сегодня: несколько строк на вебхук, вебхуков — единицы в день | Когда владелец назвал окно и оно добавлено политикой в `lib/retention.ts` (одна строка + тест), либо решил «навсегда» — тогда строку снять с этой формулировкой |
| **Текст `/privacy` §6 расходится с фактом — на ревью юристу** (#162): (1) «Request and security logs: 30 days» и «Technical data … up to 30 days» — Vercel хранит runtime-логи Hobby 1 ч / Pro 1 сутки, т.е. обещано БОЛЬШЕ реального (план Vercel не установлен — см. строку про DPA); (2) §6 не называет отправленные напоминания (факт: 30 дней после отправки, `lib/retention.ts`) и дни просмотра `watch_days` (факт: 25 месяцев под «Audience-measurement data»); (3) «Trial sessions … 30 days, then deleted or fully anonymised» — строки, привязанные к аккаунту, живут с аккаунтом как его данные и уезжают ≤30 дней после стирания. Тексты политики — решение владельца/юриста, не агента | Обещание длиннее факта по логам — формально нарушение обещания в обратную сторону; неназванные окна — неполнота §6 | Когда юрист переписал §6 (или владелец подтвердил формулировки) и правка на `/privacy` en+es влита |
| **Stripe Customer переживает стирание аккаунта** — `customers.del` при `user.deleted` не вызывается. Обработчик (#164) ставит живой подписке `cancel_at_period_end: true` и тумбстоунит customer id в `erased_customers`, но email + billing address остаются у Stripe как Customer | Ст. 17 у процессора исполнен частично: подписка гаснет, запись о клиенте — нет (инвойсы Stripe хранит как налоговые записи в любом случае, ст. 17(3)(b)) | Решение владельца (`needs:owner`): удалять Customer из обработчика (`customers.del` — необратимо, портал и история инвойсов клиента исчезают из его доступа) или не удалять и записать это в ответ пользователю. После решения — либо один вызов в `lib/erase-user.ts:eraseUser` (best-effort, как отмена подписки — и тогда `pnpm erase-user --apply` получает его автоматически), либо строка уходит с пометкой «принято» |
| **Scope PostHog personal key для стирания не проверен** (#180): `POSTHOG_PERSONAL_API_KEY` создавался под HogQL-панели (`query:read`); `lib/posthog-erase.ts` нужны `person:read` (lookup) + `person:write` (delete). Локально ключа нет, проверить можно только на стенде/проде; без `person:write` обработчик честно отвечает `skipped_forbidden` (лог + Sentry по id), person с `email` остаётся у PostHog до ручного удаления по ранбуку §4 | Ст. 17 у PostHog исполняется руками, а не кодом, пока scope не добавлен | Основная сессия: dry-run `DATABASE_URL=… POSTHOG_PERSONAL_API_KEY=… POSTHOG_PROJECT_ID=190233 pnpm erase-user <тестовый user_… стенда>` показывает `posthog: found` / `not_found` (чтение есть), затем `--apply` по тестовому пользователю — `deleted`. Если `skipped_forbidden` — `needs:owner`: добавить scope `person:write` ключу (PostHog → Settings → Personal API keys) или завести отдельный ключ и заменить переменную на обоих проектах Vercel |
| **DPA четырёх процессоров не подтверждены для нашего аккаунта** (колонка «проверить/подписать» в `references/processors.md`): Neon/Databricks (DPA предлагается, не авто), PostHog (DPA действителен только контрподписанный в приложении), Sentry (DPA требует принятия в настройках организации), Google (Data Processing Terms принимаются в GA4 Admin); плюс план Vercel (Hobby/Pro) не установлен — от него зависит обещанный ретеншен логов | Ст. 28: обработка без оформленного DPA; юридическая приёмка запуска платежей (#151) | Когда владелец подтвердил принятие каждого и в `processors.md` не осталось «проверить»/«подписать» (ops владельца, `needs:owner`) |
| The static Lab smoke (`tools/lab/static-smoke.mjs`, run by `pnpm lab:build`) is **not in CI**: nothing consumes `storybook-static/` today, and a Storybook build per PR would cost a minute for an artifact nobody opens (#78) | Nothing today — the shim in `.storybook/main.ts` is exercised by `pnpm lab` daily, and CI's `pnpm test:stories` proves the vitest side; a regression that only breaks the static build is caught the next time someone runs `lab:build` | When the static Lab gets a consumer (published bench, Chromatic, design-sync switching to it) — then `pnpm lab:build` joins `ci.yml` |
| design-sync's private Storybook config (`.design-sync/sb-config/`) carries its OWN copy of the `vitest` shim (`test-shim.ts` + alias, and it also aliases `storybook/test`, which #78 showed is unnecessary) — a second implementation of what `tools/lab/` now does for the main config | Two shims drift: a name added to one and not the other breaks one of the two builds | When the reference build's `main.ts` reuses `tools/lab/test-shim-alias.ts` and the duplicate `test-shim.ts` is deleted (touches the design-sync run notes, so it goes with the next re-sync) |
| The home hero's teaser **restarts from the beginning on every token cycle**: the 60s preview JWT dies under the loop, the player unmounts on the fatal error, fetches a fresh token from `/api/hero-preview-token` and remounts from 0 — so roughly every 90s the hero crossfades to the backdrop for a second and the teaser starts over (#128). A seamless swap would need a token change on the live element, which `<mux-player>` applies as an in-place stream reload with the catch-less `play()` of #126; continuing from where it stopped would stream the whole episode on the home page, which the 60s cap exists to prevent | Nothing functional — a periodic ~1s blink to the still on `/`, seen only by a visitor who lingers past the first ~90s | When the owner decides the hero should either loop a dedicated short teaser asset (no token cycling at all) or accept a longer preview TTL; either retires the refresh loop |
| **The fork prompt's look was picked by the agent, not the owner** (#144, Lab-first with the owner away): five genuinely different variants are on the Lab board (`Lab/Fork choice` — A bar, B cards, C sheet, D strip, E halves; `lab/fork-choice-variants.tsx`), and the product ships **A** (`components/watch/fork-choice-overlay.tsx`) as the agent's recommendation. B–E take the same props, so the swap is one decision: copy the chosen variant's markup into the product component | Nothing functional — the prompt works; the owner has not looked at it live on a device yet | When the owner picks a variant on the Lab board (keep A → drop this row; another → port it, keep the portal + the `inline` prop, re-golden) |
| **Non-default branch preloaders run at `preload="metadata"`, decided by arithmetic, not by a device measurement** (#144): the default (or the single follower) warms at `preload="auto"` from 45s out as before; a fork's other options mount only while the prompt is open, at `metadata` (manifest + first segment), and a pick promotes its option to `auto` in place. The spec's criterion — the parent rebuffering on Fast 3G in its last 15s with three candidates — was not run: no branching show exists on any bench yet. The arithmetic: three 30s buffers ≈ 90s of media in a ≤30s window is more than a 1.5 Mbps link carries; one full buffer + two first segments is not | A NON-default pick on a slow link may show a longer seam (~1s tail) than the default; on a fast link nothing | When the owner's iPhone check (foreground, wifi + cellular) on a real fork show is done: seam under ~1s on a non-default pick → drop the row; a black frame at the swap → hold the last frame / poster over the seam (decide then) |
| **Every body-portaled player overlay — the fork prompt included — is invisible in element fullscreen**: media-chrome's fullscreen target is the `<media-controller>` itself, and a `document.body` portal (required — the controller treats any click in its subtree as a play/pause gesture) is outside it. Pre-existing for the episodes / up-next / series-end overlays; for the fork it means a viewer in desktop fullscreen sees no prompt and gets the default. iOS hands fullscreen to the system player, where no overlay exists anyway | A desktop viewer who went fullscreen never sees the choice | When the overlays get a fullscreen-aware target — e.g. `fullscreenElement` on the controller pointed at the WatchShell so the portal target sits inside it, or a portal into `document.fullscreenElement ?? document.body` with the gesture receiver's click swallowed (needs a real-device check) |
| **No re-choose on a seek back** (owner decision, v1): once a fork is taken, seeking backwards inside the branch — or into the parent — does not re-arm the prompt; the viewer replays the parent to pick again (the transport's prev button on a branch goes to its parent; the episodes overlay lists the parent). Inside the parent's own window, seeking back OUT of the window closes the prompt and a pick already made survives | A viewer who regrets a pick has a two-tap detour instead of a scrub | When the owner asks for it — it is a player-only change (re-arm the prompt when `currentTime` crosses back below the fork window) with no schema impact |
| **No choice statistics in the admin panel**: which option viewers pick is recorded only implicitly, as `watch_progress` rows on the branch episodes, and no dashboard block reads them | The owner cannot see the split between options without a query | When a "Fork split" block is added to `/admin/analytics` (count distinct users per branch episode of a fork, from `watch_progress`) — no new instrumentation needed |
| **Paid-mode trial mints multiply on a branching show**: the legacy 60s preview and the free-tier tracking mint one `trial_sessions` row per (cookie, show) — fine — but the per-(IP, show) rate limit of 10 token requests per hour (`TRIAL_RATELIMIT_PER_HOUR`, `lib/trial.ts`) counts every token fetch, and a fork's prefetch + branch + reconvergence is three fetches per fork | Nothing today (payments off, the limit only degrades tracking on gated shows); in paid mode a viewer on a long interactive episode could hit the 429 on a shared IP | When payments are re-enabled: raise the limit or count per (IP, show, hour) only on the FIRST mint — decide at the re-enable checklist in the Free-pivot rule |
| Colour literals still live in two inline-style spots the style gate does not scan (#31 closed the prop / shadow / gradient layers; the gate greps `color=`, `shadow-[` and `gradient(` only): the media-chrome theme variables in `components/watch/player.tsx` (`--media-primary-color: "#f6efe4"`, `--media-range-bar-color: "#e6b366"`, the `rgba(…)` menu backgrounds, `backgroundColor: "#000"`) and the Satori text/background colours in `app/(public)/shows/[slug]/opengraph-image.tsx` (`background: "#0f0a07"`, `color: "#f6efe4"`, `rgba(230,179,102,0.7)`) | Nothing visible — they equal the palette today — but a palette change has to be repeated by hand there, and the gate will not say so | When they read `var(--color-cream)` / `PALETTE.cream` (the OG image cannot use CSS variables — Satori — so PALETTE it is) and the gate gains an inline-style pattern (`: "#…"` in a `style` object) that stays green |
| **TypeScript stays on 6.0.3, not 7** (#157, 1/3): `typescript@7` is the native compiler and its npm package exports no JavaScript compiler API (root export is `{ version }`; `typescript/unstable/*` is unstable, a stable API is promised for 7.1). `typescript-eslint@8` — the parser behind `eslint-config-next` — reads `ts.Extension` while loading, so with 7 installed `pnpm lint` dies before the first file (`TypeError: Cannot read properties of undefined (reading 'Cjs')`), while `tsc --noEmit`, vitest and `next build` (`useTypeScriptCli`) are fine. 6.0.3 is the last JS release and the bridge to 7: it passed every gate unchanged; `.github/dependabot.yml` keeps a semver-major ignore on `typescript` so the bot does not re-propose 7 weekly (details: docs/gotchas.md → «TypeScript 7 ships no JavaScript compiler API») | Nothing functional — a slower `tsc` than the native one, and the 5→7 jump stays half-done (tracked in #289 since #157 closed) | When typescript-eslint supports the TS 7 API (its peer range admits `>=7`) — i.e. after TS 7.1 stabilises the API — or the owner opts for the TS team's dual install (`typescript: npm:@typescript/typescript6` for tools + the native compiler under an alias). Then: drop the Dependabot ignore, bump `typescript`, no tsconfig change needed (verified on 7.0.2, PR #182) |
| **Кошелёк на paywall (#210): владелец ещё не выбрал вариант из пяти.** Продукт везёт A (`WalletSlotStacked` — карточный CTA основной, слот кошелька под чекбоксом согласия); B–E живут только в Lab (`lab/wallet-slot-variants.tsx`, история `Lab/Wallet slot`). Перенос — копирование разметки, пропсы одинаковые | Ничего: A рабочий. Но пять вариантов существуют именно чтобы владелец посмотрел их вживую на телефоне | Когда владелец выберет вариант вживую и он уедет в продукт — либо явно подтвердит A |
| **Google Pay на paywall не проверен на живом Android.** Stripe документирует: «Checkout Sessions using Stripe Tax only show Google Pay if you enable `shipping_address_collection`». У нас `automatic_tax: {enabled:true}` и адреса доставки нет (цифровое видео), так что кнопка Google Pay может не отрисоваться вообще | Половина запроса владельца (Android). Apple Pay это ограничение не касается | Когда на стенде на реальном Android Chrome снят `onAvailablePaymentMethodsChange` и результат доложен владельцу: либо Google Pay есть, либо владелец выбирает между `shipping_address_collection`, отключением `automatic_tax` на этой поверхности и «только Apple Pay» |
| **Гостевая (pay-first) покупка кошельком не реализована** — v1 только для авторизованных (`resolveWalletGate` → `anonymous`). Именно у гостей выигрыш от кнопки максимальный | Анонимный зритель на стене по-прежнему идёт через `/checkout` | Отдельный PR со своей репетицией на стенде: клейм аккаунта из e-mail кошелька, привязка `checkout_claim` ↔ `client_reference_id`, минт sign-in тикета |
| **`matio-staging.vercel.app` не зарегистрирован как payment method domain** (в live-аккаунте есть только `matio.tv`, `ko-fi.com` и два домена Stripe). Для Elements регистрация обязательна и делается отдельно в песочнице | Кнопка кошелька на стенде не отрисуется вообще, пока владелец не добавит домен | Когда владелец добавит домен в Stripe Dashboard (песочница) и `GET /v1/payment_method_domains` покажет `apple_pay.status=active` |
| **Оверлеи плеера (включая paywall) не видны в element fullscreen, а на iPhone в fullscreen DOM-оверлея нет вообще** — fullscreen отдаётся системному плееру. Кнопка кошелька наследует это ограничение | Зритель, смотрящий во весь экран (естественный способ на телефоне), стены с кошельком не увидит — увидит после выхода из fullscreen | Существующая строка про портальные оверлеи в fullscreen; закрывается вместе с ней |
| **Репетиция #217 на стенде — основная сессия после мержа**: две вкладки → одна открытая сессия (проверка через `checkout.sessions.list({customer, status:'open'})` тестовым ключом), оплата в одной → попытка во второй падает без списания, в песочнице одна подписка. Тестовые ключи Stripe на `matio-staging` есть и payment-method domain зарегистрирован (09–10.09); агент прогнать не мог — у него только `.env.local` с live-ключами. Поведение доказано юнитами против stateful-фейка Stripe (`app/subscribe/wallet-actions.test.ts`, «#217 — one open session per buyer») | Приёмочный пункт #217 «репетиция на стенде» | Когда основная сессия прогонит сценарий на стенде после мержа #225; результат — комментарий в PR/issue |
| **`/checkout` узнаёт об истёкшей сессии только при возврате во вкладку (`visibilitychange`) или фокусе окна (`focus`) — не при отклонённом сабмите внутри iframe Stripe.** `EmbeddedCheckoutProvider` глотает ошибки инициализации (нет `.catch`), а у Embedded Checkout нет события «сессия истекла»; проба по фокусу покрывает реальный двухвкладочный сценарий и взаимное истечение двух одномоментных create, но покупатель, оставшийся в той же вкладке и нажавший «Оплатить» на убитой сессии, увидит сообщение Stripe внутри iframe, а не нашу карточку «Попробовать снова» | Одна лишняя перезагрузка страницы для покупателя в редком сценарии; денег не трогает | Когда Stripe даст `onError`/статус-событие для Embedded Checkout, либо `onAnalyticsEvent: submit_failed` окажется надёжным сигналом для повторной пробы — проверить на репетиции |
| **Остаток третьего порядка гостевого sweep'а (#224): откат клейма после неудачного истечения может вытеснить id третьей вкладки.** Клейм (`guest_checkout_sessions`) идёт ДО истечения предыдущей сессии; если `expireIfOpen(previous)` упал при всё ещё открытой `previous` (Stripe 429/5xx), `rollBackGuestClaim` пишет `previous` обратно тем же upsert'ом. Если между клеймом и откатом успела третья вкладка, её id вытесняется: её сессия живёт, но нигде не записана до собственного истечения у Stripe (≤24 ч); то же при двойном сбое (Stripe + БД отказала на откате — тогда без записи остаётся `previous`). Логируется по id (`rolling back the claim displaced a newer session` / `rolling back the claim failed`), тест-аудит пришпиливает | Нужны отказ Stripe И третья вкладка в ту же секунду; страховка — дубль-гард `mirrorSubscription` + ручной возврат | Когда у гостя появится клиент Stripe до оплаты (sweep по `list({customer})`, таблица уходит) или Stripe научит `list` фильтру по `client_reference_id`; либо если строка `rolling back the claim` хоть раз появится в проде — тогда отдельная issue |
| **Отказ лимита `/checkout` (#227) больше не доходит до Sentry (#233).** До #233 `createAuthCheckoutSession` бросал `CheckoutRateLimitedError` наружу из server action, `onRequestError` → `Sentry.captureRequestError` давал событие на каждый отказ, и это держалось намеренно как сигнал о злоупотреблении («первый всплеск — владельцу стоит увидеть»). С #233 диспетчер `createCheckoutSession` (`app/checkout/actions.ts`) ловит ровно этот класс и отвечает кодом `rate_limited` — клиенту нужен код, текст брошенного в проде маскируется. Из action больше ничего не бросается, так что шум при флуде исчез вместе с сигналом: след отказа теперь только `console.warn("startCheckout: rate limited", { userId })` в runtime-логах Vercel, где алерта нет | Флуд по `/checkout` (скрипт, гоняющий создание сессий) лимит режет, но владелец о нём не узнаёт | Владелец решает: логов достаточно (закрыть строку) — или отказ считается явно (например `Sentry.captureMessage` с одним id, кейс в лог-аудите), отдельным issue |
| **ESLint 10 runs eslint-config-next's React trio through a compat shim** (#157, part 2): `eslint-plugin-react` 7.37.5 (last release 2025-04; jsx-eslint/eslint-plugin-react#3977 open since 2026-02), `eslint-plugin-jsx-a11y` 6.10.2 (2024-10; #1075 open) and `eslint-plugin-import` 2.32.0 (2025-06) declare `eslint` peers that stop at `^9` and still call the rule-context members ESLint 10 removed (`context.getFilename()` & co.) — `react/display-name` crashed on the first file. `eslint.config.mjs` wraps exactly those three plugin objects in `@eslint/compat`'s `fixupPluginRules` (the ESLint team's own shim); the 113 resolved rules and their levels are identical to ESLint 9's (`--print-config` diff in the part-2 PR). Details: docs/gotchas.md → «ESLint 10 removes the deprecated rule context» | Nothing today — `pnpm lint` is green and as fast as before; the cost is one devDependency and a wrapped rule context for three plugins, which would also mask a future upstream fix (or break) of exactly those calls | When `eslint-config-next` depends on ESLint-10-native versions of the three (peer ranges admitting `^10`, the upstream issues above closed) — then drop `@eslint/compat`, `fixupLegacyPlugins` in `eslint.config.mjs` and this row. Cheap check on every `eslint-config-next` bump: delete the shim locally, run `pnpm lint` |
| **Node runtime and `@types/node` stay on 24, not 26** (#157 → #289): both Vercel projects (`matio`, `matio-staging`) run Node **24.x** (`vercel project inspect`), `.nvmrc` = 24 (every workflow reads it) and `@types/node` ^24.13.6 is the newest 24.x. The 26 types were NOT taken on 2026-09-25 on purpose: types describe the runtime, and 26's would let code typecheck against APIs production does not have. Node 26 is still "Current" — Active LTS starts 2026-10-28. `.github/dependabot.yml` keeps the semver-major ignore on `@types/node` | Nothing today; the cost is staying one Node major behind the newest types until 26 is LTS | Node 26 is LTS **and** Vercel offers `26.x` → one PR: switch Node.js Version on both Vercel projects, `.nvmrc` → 26, `@types/node` → ^26, drop the Dependabot ignore (#289 §3) |
| **`@mux/mux-node` 15 is the last release under that name** (#157 → #289): since v15 the SDK is published as `@mux/ts`; `@mux/mux-node` is a same-artifact alias for v15 only (MIGRATION.md in muxinc/mux-ts). The Dependabot major-ignore on it was dropped in #157, but there will be no 16 to propose — updates stop silently once `@mux/ts` 16 ships | Mux SDK fixes after v15 never reach us while the dependency keeps the old name | Rename to `@mux/ts` (package.json + `lib/mux.ts` + `scripts/backfill-mux-policy.ts` + docs) — at the latest when `@mux/ts` 16.x appears; a formally new package name, so owner's ok (#289 §4) |
| **vitest stays on 4** (#157 part 4 → #289): `@storybook/addon-vitest` 10.6.0 declares peer `vitest ^3 \|\| ^4`; on vitest 5 its per-story viewport never reaches the page and every Lab golden renders 414 px wide instead of 960 (#220). `@vitest/browser-playwright` pins the exact vitest version, so the trio moves together; Dependabot ignores `vitest` / `@vitest/*` majors | Nothing functional; the unit suite, coverage and stories run on 4.1.x | `npm view @storybook/addon-vitest@latest peerDependencies.vitest` admits `^5` → one PR for the trio + addon, breaking changes of 5.0 checked on the unit project (#289 §1) |
| **A TestFlight build can ship WITHOUT the Clerk publishable key, and nothing stops it** (#247): `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` lived only in the gitignored `mobile/.env.local`, `eas.json → production.env` is empty, and 0.1.0 (4) proved it (its Hermes bundle carries no key). Since #247 such a build no longer crashes: `AuthProvider` mounts no Clerk, the Account tab and the locked-episode screen say «Sign-in unavailable», nobody can sign in. The EAS variable is in place since 2026-09-19 (production/preview/development — `eas env:list`), but there is no build-time guard — `app.json` is static, so nothing fails the EAS build if the variable is ever missing again (the web has that guard in `next.config.ts` for `STRIPE_PRICE_MONTHLY`) | A key-less build reaches TestFlight silently: sign-in, member episodes, continue-watching — the whole signed-in half of the app | Check before every build: `eas env:list --environment production` shows the variable, and the built bundle carries the 28-character `pk_live_…` (`strings main.jsbundle`). The durable close is a build guard — an `app.config.ts` that throws on `EAS_BUILD_PROFILE=production` without the key — an owner decision, since it turns `app.json` dynamic |
| **Prod Clerk instance has Native applications DISABLED** (#251): `clerk.matio.tv/v1/environment?_is_native=1` answers `400 native_api_disabled` (without the flag — 200), and `@clerk/expo` appends `_is_native=1` to every request — so the app's Clerk client cannot load even with the key in the bundle (`status=error`, `isLoaded` never true; the Account tab sits on its spinner). Found while reproducing #247, not caused by it | Sign-in in the app — the «signed-in» half of #247's acceptance is checked on build (5) only after this flips | The owner enables Native applications in the Clerk Dashboard (#251, `needs:owner`); check: the curl above → 200 |
| **The sign-up wall and the error pages of the landscape player render in landscape** (`mobile/src/watch/episode-feed.tsx`, #252): a horizontal show locks the whole watch screen to landscape, and a locked episode's page IS the wall (`SignupWall`) — reached by auto-advance out of the last open episode, or by a 403 from the token route — so the wall's fixed, unscrollable column (pill + 34pt title + copy + CTA + fine print) has ~390pt of height on a phone. The show page still routes a locked episode to sign-in before the player, and the sign-in the wall's CTA pushes IS portrait (the blur releases the lock — the review's fix); only the wall itself stays landscape | On a phone, an auto-advance into a locked episode shows the wall cramped, possibly clipped, in landscape; the CTA still works | A follow-up that either releases the lock to portrait while a wall / error page is current (`useOrientationLock` takes a second input) or makes the wall a ScrollView — a small player change with its own spec |
| **The splash can rotate before the root portrait lock lands** (`mobile/src/app/_layout.tsx`, #252): `app.json` allows every orientation at the OS level and the JS lock runs only once the bundle does, so a phone held sideways at launch shows the splash in landscape and snaps to portrait a beat later. `expo-screen-orientation`'s config plugin (`initialOrientation: "PORTRAIT_UP"` → `EXDefaultScreenOrientationMask`) closes it natively; the #252 spec said no plugin | Cosmetic: one rotation of the splash on a sideways launch, nothing after the lock | Add the plugin entry to `app.json` (one line, no new dependency, then `expo prebuild --clean`) once it is seen on a device — or drop this row if it never is |
| **A screen pushed over a PLAYING landscape page restarts that page on return** (`mobile/src/app/watch/[episodeId].tsx`, #252 review): the feed is unmounted on blur — the lock goes back to portrait for the pushed screen, and a mounted `FlatList` resized under it drops and re-creates its current page — and remounts on focus on the page it left (`onCurrentChange`), but not at the playhead it left: the deep link's `resumeSeconds` applies only to the deep-linked episode, any other page starts at 0. Today the only screen pushed over the player is sign-in from the WALL, whose page has no video, so nothing restarts in practice | Nothing today; the day a screen is pushed over a playing page (a share sheet route, a settings push), that episode restarts from 0 (or from the deep-link resume) when the viewer comes back | Carry the current page's playhead through `onCurrentChange` (`{index, positionSeconds}`) and seed the remounted page with it — a small feed change, when such a screen exists |
| **The Account tab's Retry reloads Clerk through an internal method** (`mobile/src/auth/clerk.tsx`, #253): `@clerk/react` 6.16 exposes no public «load again», and a `<ClerkProvider key>` remount is handed the same stuck instance (`IsomorphicClerk.getOrCreateInstance` runs in the new provider's render, before the old one's cleanup clears the singleton), so `retry()` feature-detects `IsomorphicClerk.loadHeadlessClerk()` — the constructor's own headless load path — and falls back to a fresh 8 s wait when it is absent. Every retry against #251's `400 native_api_disabled` fails again by design: the fix for that one is the vendor toggle | A Dependabot bump that renames or removes the method silently turns Retry into «wait 8 s, same error» — honest, but a viewer whose network came back has to relaunch the app to sign in | When `@clerk/react` grants a public reload (or `getOrCreateInstance` stops reusing the instance across remounts) — then `retry()` uses that and the cast goes; the weekly deps wave checks `grep -n loadHeadlessClerk mobile/node_modules/@clerk/react/dist/*.mjs` still hits |
| **osv ignore: `image-size` 1.2.1, GHSA-5p2g-fcmc-qvqq** (`mobile/osv-scanner.toml`, #256): JXL/HEIF parser infinite loop; **no fixed release exists** on any line (advisory `last_affected 2.0.2`). Pulled by `metro` 0.84.4 (`^1.0.2`) ← `@expo/metro` ← `expo`. Build-time only — Metro reads the dimensions of the project's OWN assets; verified absent from the exported iOS bundle | nothing at runtime; the nightly `security` run goes red again on the review date | **Review 2026-12-20** (`ignoreUntil`): re-check the advisory for a fixed version → `npm update image-size` or a scoped override, then delete the entry |
| **osv ignore: `image-size` 1.2.1, GHSA-w3rx-r6r6-pgpr** (`mobile/osv-scanner.toml`, #256): ICNS parser infinite loop — same package, same exposure and same fix path as the row above | same | **Review 2026-12-20**, together with the row above |
| **osv ignore: `stream-json` 1.9.1, GHSA-528h-pc64-c93x** (`mobile/osv-scanner.toml`, #256): O(depth²) DoS in the pick/ignore/filter/replace filters. Pulled by `jayson` 4.3.0 (`^1.9.1`) ← `@solana/web3.js` ← `@clerk/clerk-js` ← `@clerk/expo`. Unreachable: jayson requires only `StreamValues` + `Verifier`, from its server-side `lib/utils.js`, which web3.js never loads; `stream-json`, `jayson` and `@solana/web3.js` are all absent from the exported iOS bundle. The fix (3.5.0) is ESM-only / node ≥22 / moved entry points — jayson's CJS `require` cannot load it, so no override | nothing at runtime; nightly goes red on the review date | **Review 2026-12-20**: closes when `jayson` moves to stream-json 3 or `@clerk/clerk-js` drops the Solana wallet adapters |
| **osv ignore: `decode-uri-component` 0.2.2, GHSA-vcc3-ghjq-m6fr — THIS ONE SHIPS IN THE APP** (`mobile/osv-scanner.toml`, #256): exponential decoding of malformed percent-encoded input. Pulled by `query-string` 7.1.3 (`^0.2.2`) ← `expo-router`, which runs an incoming path's query through it (`react-navigation/core/getStateFromPath.js`) — present in the exported iOS bundle. Not overridden because the only fix, 0.5.0, is ESM-only while query-string 7 does a CJS `require()` and calls the export as a function: the override would break deep-link parsing for everyone. Exposure: a client-side stall on the device that opens a crafted deep link — no server, no data, no other user | a real but low runtime exposure, accepted knowingly — not build noise like the three rows above | **Review 2026-12-20**: closes when `expo-router` drops `query-string` 7 (check on every Expo SDK bump); if the app ever feeds query-string from a source other than its own deep links, re-assess at once |
| **`.gitleaksignore`: two fingerprints for `lib/guest-checkout-sessions.test.ts:52`** (#256): commits `e1d93bc2` (the #231 branch) and `f40d7795` (its squash), rule `generic-api-key`, a made-up UUID fixture that never was a credential. The fixture now looks fake, but gitleaks scans the whole history of every branch, so the old commits keep the line forever | nothing | Never, short of a history rewrite — the row stays so the file is not mistaken for a place to park findings: only never-real values go there, a real secret is rotated |
| **Four dependency overrides pin a transitive version by hand** (#256): `mobile/package.json` → `xcode>uuid` and `jayson>uuid` `^11.1.1` (parents ask `^7.0.3` / `^8.3.2`; both only call `v4()`, 11.x keeps CJS + that export — smoke-tested under Node; the npm `uuid` is not in the app bundle at all); root `package.json` → `@clerk/shared@3>js-cookie` `3.0.8` (parent pins exactly `3.0.5`; `@clerk/shared` 4 already ships 3.0.8) and `@esbuild-kit/core-utils>esbuild` `0.25.12` (parent asks `~0.18.20`; dev-only under `drizzle-kit`, deduped onto the esbuild drizzle-kit already uses — `drizzle-kit check` + `generate` pass) | an override outlives its reason silently: once the parent catches up it pins an OLD version | Drop each one when its parent's own range reaches the fixed version — check on every Dependabot wave and at the 2026-12-20 review (`npm ls uuid` in `mobile/`, `pnpm why js-cookie esbuild` in the root) |
