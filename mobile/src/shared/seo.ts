// The site's per-language URL rule — English bare, Spanish under /es — for
// the legal links Settings opens. Same single-relative-path rule as
// ./design.ts; lib/seo.ts has no imports at all.
export { localizedPath } from "../../../lib/seo";
